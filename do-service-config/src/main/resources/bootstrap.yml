server:
  port: ${PORT:8888}

info:
  component: Config Server

---
spring:
  profiles: git
  cloud:
    config:
      label: cloud-config
      server:
        git:
          uri: https://github.com/ridlafadilah/do-micro.git
          clone-on-start: true

---
spring:
  profiles: native
  cloud:
    config:
      server:
        native:
          searchLocations: file:///config_shared/